## 2026-07-04 白噪音哄睡小程序 UI 重设计

### 图标重新生成
- 使用 ImageGen 生成 3x3 精灵图（9个精致线框图标）
- 使用 Python/PIL 裁剪并去除灰色背景，生成透明 PNG
- 生成 `_active` 版本（粉色底 + 白色图标）
- 清理旧图标文件，保留新图标文件

### 样式修复
- 修复 `app.wxss` 颜色格式（去除多余字符）
- 恢复原始配色值（#FBF7F2, #E8C4B8 等）
- 修复 `sound-card.wxss` 点击特效和播放态颜色
- 修复 `pages/index/index.wxss` 4列网格布局
- 修复 `cry-detector.wxss` 录音按钮和结果卡片样式
- 添加水波纹动画和播放指示器动画

### 架构恢复
- 恢复使用 `tips-marquee`、`cry-detector`、`sound-card` 组件
- 修复 `index.js` 全局状态回调注册
- 恢复 `app.json` 默认导航栏配置
